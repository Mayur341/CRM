@model CRM.Models.Lead

@{
    ViewData["Title"] = "Edit";
}

<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
            
        }

        h1, h4 {
            color: #003153; /* Prussian blue */
            font-weight: bold;
        }

        .form-group label {
            font-weight: bold;
            color: #003153; /* Prussian blue */
        }

        .form-control {
            border: 1px solid #ced4da;
            box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1); /* Shadow around input fields */
        }

        .btn-primary {
            background-color: #003153; /* Prussian blue */
            border-color: #003153; /* Prussian blue */
        }

            .btn-primary:hover {
                background-color: #002b48; /* Darker shade of Prussian blue */
                border-color: #002b48; /* Darker shade of Prussian blue */
            }

        .text-danger {
            color: red;
        }
    </style>
</head>
<body>

    <div class="container">

        <hr />
        <div class="row">
            <div class="col-md-8">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="LeadID" />

                    <div class="form-group">
                        <label asp-for="ClientID" class="control-label"></label>
                        <input id="ClientID" name="ClientID" class="form-control" value="@ViewBag.ClientID" readonly />
                    </div>

                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label asp-for="LeadName" class="control-label"></label>
                            <input asp-for="LeadName" class="form-control" />
                            <span asp-validation-for="LeadName" class="text-danger"></span>
                        </div>
                        <div class="form-group col-md-6">
                            <label asp-for="LeadDate" class="control-label"></label>
                            <input asp-for="LeadDate" class="form-control" />
                            <span asp-validation-for="LeadDate" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="LeadDescription" class="control-label"></label>
                        <textarea asp-for="LeadDescription" class="form-control"></textarea>
                        <span asp-validation-for="LeadDescription" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="LeadStageID" class="control-label"></label>
                        <select asp-for="LeadStageID" class="form-control" asp-items="ViewBag.LeadStageID"></select>
                        <span asp-validation-for="LeadStageID" class="text-danger"></span>
                    </div>

                    <div class="form-group d-flex justify-content-center align-items-center ">
                        <input type="submit" value="Save" class="btn btn-primary" />
                        <a asp-action="Index" class="btn btn-secondary ml-2">Back to List</a>
                    </div>

                </form>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        @section Scripts {
            @{
                await Html.RenderPartialAsync("_ValidationScriptsPartial");
            }
        }
    </script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>

</body>
</html>
